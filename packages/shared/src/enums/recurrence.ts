export enum RecurrenceType {
  None = 'none',
  Daily = 'daily',
  Weekly = 'weekly',
  Monthly = 'monthly',
  Yearly = 'yearly',
  Custom = 'custom',
}

export enum RecurrenceEndType {
  Never = 'never',
  AfterCount = 'after_count',
  OnDate = 'on_date',
}
